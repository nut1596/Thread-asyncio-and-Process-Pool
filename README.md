# 🧵⚡ การจำลองงาน I/O ด้วย Thread, asyncio และ Process Pool

## 📌 วัตถุประสงค์ของโครงงาน

โครงงานนี้มีจุดประสงค์เพื่อศึกษาและเปรียบเทียบการทำงานแบบ Concurrency ในภาษา Python โดยใช้ 3 วิธี ได้แก่:

- Thread
- asyncio
- Process Pool

โดยจำลองงานประเภท I/O-bound ด้วย `sleep(2)` จำนวน 5 งาน เพื่อดูความแตกต่างด้านประสิทธิภาพและเวลาในการทำงาน

---

# 🧠 แนวคิดพื้นฐาน

## 🔹 I/O-bound คืออะไร?

งานประเภท I/O-bound คือ งานที่ใช้เวลารอ เช่น:

- รอโหลดข้อมูลจากอินเทอร์เน็ต
- อ่าน/เขียนไฟล์
- รอฐานข้อมูลตอบกลับ
- sleep()

งานลักษณะนี้มักไม่ได้ใช้ CPU หนัก แต่เสียเวลารอ

---

# 🛠 วิธีการทดลอง

## เงื่อนไขการทดลอง

- สร้างงานทั้งหมด 5 งาน
- แต่ละงานใช้เวลา 2 วินาที (`sleep(2)`)
- วัดเวลาในการทำงานรวม
- เปรียบเทียบผลลัพธ์

---

# 🔵 1) Thread

ใช้ `threading.Thread`

### ลักษณะเด่น
- ใช้หน่วยความจำร่วมกัน (Shared Memory)
- เหมาะกับงาน I/O-bound
- มีข้อจำกัดเรื่อง GIL (Global Interpreter Lock)

### หลักการทำงาน
สร้างหลาย thread ทำงานพร้อมกัน ทำให้เวลารวมลดลง

---

# 🟢 2) asyncio

ใช้ `async/await` และ `asyncio.gather()`

### ลักษณะเด่น
- ไม่ใช้ thread จริง
- ใช้ Event Loop
- มีประสิทธิภาพสูงกับงาน I/O จำนวนมาก

### หลักการทำงาน
เมื่อ task หนึ่งรอ (await) ระบบจะสลับไปทำ task อื่นทันที

---

# 🟣 3) Process Pool

ใช้ `concurrent.futures.ProcessPoolExecutor`

### ลักษณะเด่น
- ใช้หลาย process
- ใช้ CPU ได้หลาย core
- ไม่มีปัญหา GIL
- มี overhead สูงกว่า thread

เหมาะกับงาน CPU-bound มากกว่า I/O-bound

---

# ⏱ ผลการทดลอง

| วิธีการ | เวลาที่ใช้ (โดยประมาณ) |
|----------|--------------------------|
| Thread | ~2.01 วินาที |
| asyncio | ~2.02 วินาที |
| Process Pool | ~2.15 วินาที |

---

# 🔎 วิเคราะห์ผลลัพธ์

หากทำงานแบบปกติ (ไม่มี concurrency):

5 งาน × 2 วินาที = 10 วินาที

แต่เมื่อใช้ Concurrency:

ใช้เวลาประมาณ 2 วินาทีเท่านั้น

แสดงให้เห็นว่า:

- ทั้ง Thread และ asyncio เหมาะกับงาน I/O-bound
- Process Pool มี overhead เพิ่มจากการสร้าง process
- สำหรับงาน I/O แบบนี้ Process ไม่ได้เร็วกว่า Thread

---

# 📊 เปรียบเทียบข้อดีข้อเสีย

| วิธี | เหมาะกับงาน | ข้อดี | ข้อเสีย |
|------|-------------|--------|----------|
| Thread | I/O-bound | ใช้งานง่าย | ติด GIL |
| asyncio | I/O-bound จำนวนมาก | เร็วและประหยัดทรัพยากร | โค้ดซับซ้อนกว่า |
| Process Pool | CPU-bound | ใช้หลาย core ได้ | มี overhead สูง |

---

# 🧩 สรุป

ในการจำลองงาน I/O ด้วย sleep():

- Thread และ asyncio ให้ผลลัพธ์ใกล้เคียงกัน
- Process Pool ไม่ได้ให้ประโยชน์เพิ่มมากนักในงานลักษณะนี้
- หากเป็นงานคำนวณหนัก (CPU-bound) Process Pool จะมีประสิทธิภาพดีกว่า

---
# ผู้จัดทำ 
# นาย อาณัส อาเก๊ะ รหัสนักศึกษา 6810110755